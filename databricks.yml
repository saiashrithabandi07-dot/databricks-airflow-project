bundle:
  name: sales_pipeline

resources:
  jobs:
    sales_job:
      name: Sales_ETL_Job
      tasks:
        - task_key: run_notebook
          notebook_task:
            notebook_path: ./Notebooks/sales.py
